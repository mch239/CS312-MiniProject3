<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CS312 Mini-Project</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <%- include('public/partials/header.ejs') %>
    <h1 class="title">Get ready to BLOG</h1>
    <div class="form-container">
      <% if (locals.isEdit) { %>
      <p class="form-text">Need to edit?</p>
      <% }else{ %>
      <p class="form-text">Let's blog the day away!</p>
      <% } %>

      <form
        action="<%= locals.isEdit ? `/edit/${locals.blogDetails?.id}` : '/home' %>"
        method="post"
      >
        <label for="bTitle">What would you like your TITLE to be?</label>
        <input
          type="text"
          id="bTitle"
          name="blogTitle"
          placeholder="Hello..."
          required
        />

        <label for="bDes">Just type here</label>
        <textarea
          type="text"
          id="bDes"
          name="blogDes"
          placeholder="..."
          rows="15"
          required
        >
        </textarea>
        <input
          type="submit"
          value="<%= locals.isEdit ? 'EDIT' : 'SUBMIT' %>"
        />
      </form>
    </div>
    <%- include('public/partials/footer.ejs') %>
  </body>
</html>
